"""Reusable string-level primitives shared by detection families.

Pure functions over domain strings — no dependency on vigil.models: PSL name
decomposition (`names`), typo-permutation generation (`permutations`), and
homoglyph/bitsquat matching (`homoglyphs`).
"""
